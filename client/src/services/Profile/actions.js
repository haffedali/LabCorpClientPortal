import {
    UPDATE_FAILED,
    UPDATE_SUCCESS,
    UPDATE_PENDING
} from './actionTypes';


export const updateAttempt = (userInfo) => {
    return (dispatch) => {
        // Make update api call
    }
}
