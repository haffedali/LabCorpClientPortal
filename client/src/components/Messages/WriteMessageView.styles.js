import {makeStyles} from '@material-ui/styles';

export const useStyles = makeStyles((theme) => ({
    toInputContainer:{
        padding: '.5vw'
    },
    toInput: {
        width: '60vw',
        padding: '.5vw'
    },
    subjectInput:{},
    bodyTextField:{
        width: '65vw',
        padding: '.5vw',
    },
    container:{}
  }));