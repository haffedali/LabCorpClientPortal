import { makeStyles } from '@material-ui/core/styles';

export const useStyles = makeStyles((theme) => ({
    header: {
        marginTop: '10%',
        marginLeft: '40%',
        textAlign: 'left',
    },
    logo: {
        marginRight: '65%',
        height: '300px',
        width: '300px',
    }
}));